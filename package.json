{
  "name": "the-saviour-workspace",
  "version": "1.0.0",
  "description": "Monorepo workspace for The Saviour - Emergency & Disaster Response Platform",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "seed": "npm run seed --prefix server",
    "seed-missions": "node server/src/seedMissions.js",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
